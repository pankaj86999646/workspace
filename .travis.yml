jobs:
  include:
    - stage: warm_cache
      script:
        - echo "new file" > A.txt
      workspaces:
        create:
          name: ws1
          paths:
            - A.txt
    - stage: use_cache
      workspaces:
        use: ws1
      script:
        - cat A.txt || true
